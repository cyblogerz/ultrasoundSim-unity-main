using Core.Display.Interfaces;
using UnityEngine;

namespace Core.Display
{
    public sealed class DisplayPipeline
    {
        public static ITextureSource StandardDisp()
        {
            GameObject probe = GameObject.FindGameObjectWithTag("Probe");
            if (probe != null)
            {
                
            }
        }
    }
}